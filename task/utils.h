#ifndef UTILS_H
#define UTILS_H

#include "./config.h"

void sort_tasks_descending_by_delay(Task arr[], size_t arr_size,
                                    unsigned short elems_quantity_to_sort);

#endif
